export type ContactValues = { email: string; firstName: string; lastName: string };

export type ShippingValues = {
  address1: string;
  address2: string;
  city: string;
  state: string;
  zip: string;
  country: string;
};
