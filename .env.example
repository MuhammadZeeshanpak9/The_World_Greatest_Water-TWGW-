# This project has no backend — there are currently no required environment
# variables. The one below is optional and only affects metadata generation
# (canonical URLs, OpenGraph tags, sitemap.xml). Falls back to a sensible
# default if unset.

NEXT_PUBLIC_SITE_URL=https://theworldsgreatestwater.com
